services:

  backend:
    build:
      context: ./backend

    container_name: eaas-backend

    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend

    container_name: eaas-frontend

    ports:
      - "3000:3000"

    depends_on:
      - backend
